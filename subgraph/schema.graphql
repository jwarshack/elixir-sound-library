type Sound @entity {
  id: ID!
  tokenID: BigInt!
  tokenURI: String
  owner: User!
  licenseCount: BigInt!
  licensees: [User!]!
}

type User @entity {
  id: ID!
  sounds: [Sound!]! @derivedFrom(field: "owner")
  licenses: BigInt!
}
